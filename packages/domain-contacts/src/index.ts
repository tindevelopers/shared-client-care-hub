// Skeleton placeholder — the contacts domain logic lands with the
// crm-packages milestone. The directory exists from Milestone 1 so the
// dependency-cruiser R5 rules, the rules-fire fixtures, and the domain-set
// meta-test bind to real packages from day one.
export {};
