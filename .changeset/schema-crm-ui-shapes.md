---
"@tindevelopers/schema-crm": minor
---

Add canonical contact list, suppression, and campaign `sent` status shapes (contact groups, contact suppressions, campaign lifecycle) backing the `ui-crm` full-screen extraction.
