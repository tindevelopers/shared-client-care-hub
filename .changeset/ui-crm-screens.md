---
"@tindevelopers/ui-crm": major
---

Ship the CRM hub UI as 1.0.0: full-screen extraction of the contacts, lists, suppression, and campaigns screens from the product app into installable components, with extension seams for embedding apps and a `./testing` subpath export for consumer test harnesses. First stable release; consumers should move from unpublished `0.1.0` previews to the 1.0 line.
